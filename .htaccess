RewriteEngine On

# Se il file o la directory esistono, servili normalmente
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Altrimenti passa tutto a index.php
RewriteRule ^ index.php [L,QSA]